export const SUPPORTED_MENU_CODES = new Set([
  'dashboard',
  'relic',
  'relic:list',
  'inventory',
  'inventory:outbound-apply',
  'inventory:outbound-approve'
])

export const SUPPORTED_MENU_PATHS = new Set([
  '/dashboard',
  '/relic',
  '/relic/list',
  '/inventory',
  '/inventory/outbound/apply',
  '/inventory/outbound/approve'
])
